#include <iostream>

#include "token.hpp"
#include "lexer.hpp"

using namespace std;

int main() {
    string str;

    cin >> str;
    
    // lexer l{};

    // std::regex& reg = l.patterns.at("identifier").re;

    // bool matched = regex_match(str,reg);
    // cout << (matched?"yes":"no") << endl;
    cout << endl;
    return 0;
}
